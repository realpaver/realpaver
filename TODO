-------------------------------------------------------------------------------
- Create a DAG from a .nl file

-------------------------------------------------------------------------------
- Convergence lente sur ce problème...
   Pourquoi ? C'est convexe et les dérivées sont linéaires.

   Variables
      x in [-1, 2],
      y in [-3, 7];
   Objectives
      MIN x^2 - 2*x*y + y^2;

-------------------------------------------------------------------------------
- Exemple d'un least square: MIN f = somme_i (g(ti, x) - yi)^2
  avec les données (ti, yi)

  Introduire de nouvelles variables pour décomposer le problème, e.g.:
  zi = g(ti, x)

  L'objectif devient MIN somme_i (zi - yi)^2

  Résolution: (1) Split sur (x, z). (2) Ajouter des contracteurs sur les
  zi = g(ti, x), e.g. HC4 et PolytopeHull.

  Dans le BOSolver, on n'a quand même pas besoin de preuve d'existence
  pour mettre à jour la upper bound.

  Idée plus générale dérrière: comment décomposer un DAG? Quels noeuds
  faut-il considérer comme des variables?
  
  
  Introduction de variables intermédiaires et contacteurs et splits

-------------------------------------------------------------------------------
- Comparaison des relaxations

-------------------------------------------------------------------------------
- Relaxations quadratiques et résolution par Gurobi : arithmétique affine vers quadratique ou formes de Taylor ordre 2 ? Ou appliquer RLT sur formes quadratiques ?

-------------------------------------------------------------------------------
- Tester autres solveurs locaux (Raphael ?)
