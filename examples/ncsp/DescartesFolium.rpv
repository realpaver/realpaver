# In: W. Older and A. Vellino.
#     Constraint Arithmetic on Real Intervals.
#     Constraint Logic Programming: Selected Research, 1993, MIT Press.

Variables
   x in [0, 2],
   y in [0, 2];

Constraints
#   (x^2)/y + (y^2)/x == 2,
   x^3 + y^3 == 2*x*y,
   y == exp(-x);
